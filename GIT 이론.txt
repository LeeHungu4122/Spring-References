to2.kr/a5H

git vash 검색 => git 프로그램 실행.

git은 버전관리프로그램
 ==> 손쉬운 세이브, 협업

clear
ls => 현재파일리스트

pwd => 현재위치 나타냄

mk -> make

mkdir ~ => 경로만들기(폴더 생성됨)

rmdir => 경로삭제(폴더 삭제됨)

cd => change dir

cd git, cd ~(현재경로 쓴 후)/git/ (앞글자 2자정도 쓴 후 tab을 누르면 경로가 자동 완성)


vim a.txt => 텍스트파일생성

(글자 쓴 후)esc 누르고
:w 쓴 후 엔터누르면 저장됨.

a를 누르면 다시 글 작성가능하고 esc누르면 저장가능해짐.

:wq 는 저장후 끝내기

git init ==> 로컬저장소 생성.(보안폴더?)

git 폴더생성 후 tu1폴더 생성하면 그 안에 git 숨김폴더생성되고 과거기록도 저장된다.

git add ~~

git status

git commit -m "~~"(설명 기입) => 파일 변경사항을 저장하고 설명에 변경 사항을 기입.(과거의 기록을 알 수 있다.)




수업페이지 입문에서 git설치에서 windows를 콘솔로 지정해야..

history

cat 파일명 ==> 파일안에있는 내용 출력



git branch => 버전? 으로 보면 됨.

